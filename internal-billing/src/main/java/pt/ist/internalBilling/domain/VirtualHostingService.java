package pt.ist.internalBilling.domain;

public class VirtualHostingService extends VirtualHostingService_Base {
    
    VirtualHostingService(String title, String description) {
        setTitle(title);
        setDescription(description);
    }
    
}
